<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/movies_menu">

    <fragment
        android:id="@+id/movies_menu"
        android:name="com.lefarmico.moviesfinder.fragments.MovieFragment"
        android:label="fragment_movie"
        tools:layout="@layout/fragment_movie" />
    <fragment
        android:id="@+id/series_menu"
        android:name="com.lefarmico.moviesfinder.fragments.SeriesFragment"
        android:label="fragment_series"
        tools:layout="@layout/fragment_series" />
    <fragment
        android:id="@+id/favorites_menu"
        android:name="com.lefarmico.moviesfinder.fragments.ProfileFragment"
        android:label="ProfileFragment" />
</navigation>